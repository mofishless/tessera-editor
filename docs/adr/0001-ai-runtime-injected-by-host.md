# AI Runtime 由宿主注入，组件自身不发网络请求

组件家族内置 AI 交互骨架（斜杠 AI 命令、划词 Improve、流式插入、diff 接受/拒绝、写回协议、人/Agent 归因），但模型调用能力（AI Runtime）一律由宿主注入：各宿主的模型供应商、密钥、网关与审计要求不同，组件绑死任何供给方会毁掉"公司统一组件"的通用性。Runtime 需同时覆盖各家 HTTP API 与本地 CLI（如用户本机安装的 codex cli）两类供给。
